# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Node / TypeScript
node_modules/
typescript/dist/

# Desktop extension build artifacts (server/*.js are copied from typescript/dist)
desktop-extension/server/
desktop-extension/*.dxt
# NOTE: odoo-connector.mcpb is intentionally committed so it can be distributed
# via a direct download link (see desktop-extension/README.md).

# Secrets
.env
